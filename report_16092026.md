# Báo cáo ngày 16/09/2026

## 1. Tổng quan công việc

Trong ngày 16/09/2026, công việc tập trung vào giai đoạn chuẩn bị dữ liệu và kiểm thử các thành phần nền tảng cho pipeline **Bilingual Voice Agent with Open-Set Language Identification**. Trọng tâm chính là bài toán **Language Identification (LID)**, trong đó hệ thống cần phân biệt được tiếng Việt, tiếng Anh và các ngôn ngữ ngoài phạm vi hỗ trợ.

Bên cạnh phần dữ liệu cho LID, một số module quan trọng của pipeline voice agent cũng đã được kiểm tra bước đầu, gồm **Voice Activity Detection (VAD)** bằng Silero VAD và **Text-to-Speech (TTS)** bằng edge-tts. Các thử nghiệm này giúp đánh giá sớm khả năng tích hợp các thành phần audio trước khi xây dựng pipeline hoàn chỉnh.

## 2. Các nhiệm vụ chính đã triển khai

### 2.1. Tìm hiểu và chuẩn bị dữ liệu cho LID

- Tìm hiểu về các bộ dataset liên quan đến bài toán **Language Identification (LID)**.
- Tìm và tải về các dataset liên quan trong việc huấn luyện mô hình LID.
- Dataset đã chuẩn bị bao gồm **Google FLEURS** với 7 set nhỏ:
  - Tiếng Việt;
  - Tiếng Anh;
  - 5 ngôn ngữ khác: Trung, Nhật, Hàn, Pháp, Thái.
- 5 ngôn ngữ ngoài Việt/Anh được gom thành tập các ngôn ngữ không hỗ trợ.

Việc chọn Google FLEURS phù hợp với giai đoạn hiện tại vì dataset này có nhiều ngôn ngữ, định dạng tương đối thống nhất và có thể dùng để tạo nhanh một baseline cho LID. Trong đó, tiếng Việt và tiếng Anh sẽ đóng vai trò là hai ngôn ngữ chính mà hệ thống cần hỗ trợ, còn các ngôn ngữ Trung, Nhật, Hàn, Pháp, Thái được dùng để xây dựng nhóm **OTHER**.

Mục tiêu của cách tổ chức này là chuẩn bị cho hướng **Open-Set LID**, tức là mô hình không chỉ phân biệt tiếng Việt và tiếng Anh, mà còn cần nhận biết khi input thuộc nhóm ngôn ngữ không được hỗ trợ. Đây là bước quan trọng để tránh trường hợp hệ thống ép mọi audio đầu vào thành tiếng Việt hoặc tiếng Anh, dẫn đến lỗi ở các bước ASR, LLM và TTS phía sau.

### 2.2. Cài đặt và thử nghiệm Silero VAD

- Cài đặt thư viện **Silero VAD**.
- Thử nghiệm chức năng **VAD** để phục vụ bước phát hiện đoạn speech trước khi chạy các module tiếp theo.

Silero VAD được kiểm tra nhằm đánh giá khả năng phát hiện đoạn có giọng nói trong audio. Đây là thành phần cần thiết ở đầu pipeline vì hệ thống không nên chạy LID hoặc ASR trên các đoạn im lặng, nhiễu nền hoặc đoạn audio không có speech rõ ràng.

Vai trò dự kiến của VAD trong pipeline:

- loại bỏ các khoảng im lặng trước và sau câu nói;
- xác định thời điểm bắt đầu và kết thúc speech;
- giảm số lượng đoạn audio không cần xử lý;
- hỗ trợ bước kiểm tra chất lượng audio trước khi đưa vào LID.

Kết quả thử nghiệm ban đầu cho thấy Silero VAD có thể được dùng làm module phát hiện speech trong prototype. Tuy nhiên, cần tiếp tục kiểm tra thêm với các trường hợp audio ngắn, nói nhỏ, có nhiễu nền hoặc có khoảng dừng giữa câu để đánh giá độ ổn định trong bối cảnh thực tế.

### 2.3. Cài đặt và thử nghiệm Text-to-Speech

- Cài đặt thư viện **edge-tts** để kiểm tra chức năng **Text-to-Speech (TTS)** với một vài script có sẵn.
- Kết quả ban đầu:
  - TTS tiếng Việt khá tốt.
  - Khi chuyển sang tiếng Anh, âm thanh thường bị vỡ tiếng hoặc tự chuyển hẳn sang giọng Anh.

Việc thử nghiệm edge-tts giúp đánh giá nhanh khả năng sinh giọng nói cho phản hồi của voice agent. Với nội dung hoàn toàn bằng tiếng Việt, chất lượng âm thanh ban đầu tương đối tốt và có thể dùng để demo. Tuy nhiên, khi câu phản hồi có xen tiếng Anh hoặc chuyển từ tiếng Việt sang tiếng Anh, kết quả chưa ổn định. Một số trường hợp bị vỡ tiếng, phát âm không tự nhiên hoặc chuyển hẳn sang giọng tiếng Anh.

Vấn đề này cần được lưu ý vì project hướng tới voice agent song ngữ Việt-Anh. Trong hội thoại thực tế, người dùng có thể dùng các câu pha trộn như tên bài hát, tên ứng dụng, địa danh, thương hiệu hoặc lệnh tiếng Anh ngắn trong câu tiếng Việt. Nếu TTS không xử lý tốt phần multilingual, trải nghiệm phản hồi bằng giọng nói sẽ bị ảnh hưởng.

## 3. Nhận xét và cân nhắc

- Có thể cần cân nhắc dùng các mô hình TTS cho multilingual.
- Trọng tâm cần lưu ý là khả năng xử lý ngôn ngữ Việt-Anh, đặc biệt trong các trường hợp có chuyển đổi giữa tiếng Việt và tiếng Anh.

Từ các thử nghiệm trong ngày, có thể thấy pipeline hiện tại cần ưu tiên hai hướng song song. Thứ nhất là chuẩn hóa dữ liệu cho LID để sớm có tập train/validation/test rõ ràng cho ba nhóm `VI`, `EN` và `OTHER`. Thứ hai là tiếp tục kiểm tra các module audio đầu vào và đầu ra, đặc biệt là VAD và TTS, để đảm bảo pipeline voice agent có thể hoạt động ổn định khi tích hợp end-to-end.

Đối với phần TTS, edge-tts có thể tiếp tục được dùng cho prototype ban đầu, nhưng cần cân nhắc thêm các lựa chọn khác nếu hệ thống yêu cầu phản hồi song ngữ hoặc code-switching tự nhiên hơn. Đặc biệt, cần kiểm tra các mô hình TTS multilingual hoặc các cách tách câu theo ngôn ngữ trước khi tổng hợp giọng nói.

## 4. Hướng công việc tiếp theo

- Chuẩn hóa lại cấu trúc thư mục dữ liệu cho LID theo các nhóm `VI`, `EN` và `OTHER`.
- Kiểm tra số lượng mẫu, định dạng audio và metadata của từng subset trong Google FLEURS.
- Xây dựng script tiền xử lý dữ liệu cơ bản cho bước huấn luyện LID.
- Thử nghiệm thêm Silero VAD với audio có độ dài và chất lượng khác nhau.
- Tiếp tục đánh giá edge-tts với các câu tiếng Việt, tiếng Anh và câu pha trộn Việt-Anh.
- Tìm hiểu thêm các lựa chọn TTS multilingual phù hợp hơn cho ngữ cảnh voice agent song ngữ.

## 5. Kết luận

Trong ngày 16/09/2026, các công việc chính đã tập trung vào việc chuẩn bị dữ liệu cho bài toán LID và kiểm thử các thành phần audio cơ bản của hệ thống. Google FLEURS đã được chọn làm nguồn dữ liệu ban đầu cho tiếng Việt, tiếng Anh và nhóm ngôn ngữ không hỗ trợ. Silero VAD đã được cài đặt và thử nghiệm để phục vụ phát hiện speech. edge-tts cũng đã được kiểm tra cho chức năng TTS, cho kết quả khá tốt với tiếng Việt nhưng còn hạn chế khi xử lý nội dung có tiếng Anh hoặc pha trộn Việt-Anh.

Các kết quả này tạo nền tảng cho bước tiếp theo là xây dựng baseline LID, hoàn thiện xử lý audio đầu vào và lựa chọn giải pháp TTS phù hợp hơn với mục tiêu voice agent song ngữ.

