# Báo cáo tìm hiểu Speech Understanding
**Ngày:** 14/09/2026  

## 1. Bài toán ASR & Vấn đề Alignment

- **Định nghĩa bài toán:** Automatic Speech Recognition (ASR) ánh xạ chuỗi đặc trưng âm thanh đầu vào $X=(x_1, x_2, \ldots, x_T)$ thành chuỗi token văn bản $Y=(y_1, y_2, \ldots, y_U)$, mục tiêu là tìm $\hat{Y} = \arg\max_Y P(Y|X)$.
- **Thách thức cốt lõi (Alignment Problem):** Số lượng audio frame $T$ lớn hơn rất nhiều so với số lượng token văn bản $U$ ($T \gg U$). Tín hiệu tiếng nói liên tục không có ranh giới token hay khoảng trắng rõ ràng, đòi hỏi mô hình phải tự tìm ra phân bố frame cho từng token.
- **Chuyển dịch sang End-to-End (E2E):** Thay thế cấu trúc modular cổ điển phức tạp (gồm Acoustic Model tách biệt, Pronunciation Lexicon, HMM, Language Model) bằng một mạng nơ-ron thống nhất học trực tiếp $P(Y|X)$ từ biểu diễn âm thanh sang ký tự/từ.
- Hai hướng tiếp cận E2E chủ đạo: mô hình tối ưu theo căn chỉnh đơn điệu (như CTC, Transducer) và mô hình sinh tự hồi quy (Encoder-Decoder với Attention).

## 2. Các kiến trúc End-to-End ASR cốt lõi

- **Connectionist Temporal Classification (CTC):**
  - Giới thiệu ký hiệu đặc biệt **blank (`_`)** để mạng nơ-ron biểu thị trạng thái "chờ" tại các frame chưa cần phát sinh token mới.
  - **Hàm collapse:** Gộp các nhãn lặp liên tiếp rồi xóa blank để nén chuỗi frame dài thành chuỗi token ngắn (ví dụ: `_ x x _ i i _ n _` $\rightarrow$ `x i n` $\rightarrow$ "xin").
  - **CTC Loss:** Tính tổng xác suất của mọi đường đi hợp lệ (valid paths) có thể collapse thành transcript đúng bằng quy hoạch động:  
    $P(Y|X) = \sum_{\pi: \mathcal{B}(\pi)=Y} P(\pi|X)$.
  - Giả định các frame đầu ra độc lập có điều kiện; tốc độ giải mã rất nhanh, phù hợp căn chỉnh tiến đều theo thời gian (monotonic), nhưng phụ thuộc nhiều vào encoder hoặc external LM để học ngữ cảnh ngôn ngữ.
- **Encoder-Decoder với Attention:**
  - Sinh transcript tự hồi quy (autoregressive):  
    $P(Y|X) = \prod_{u} P(y_u \mid y_1, \ldots, y_{u-1}, X)$.
  - Decoder đóng vai trò mô hình ngôn ngữ nội tại mạnh mẽ giúp câu văn mạch lạc, nhưng suy luận tuần tự có độ trễ cao hơn CTC và chịu rủi ro sai lệch phơi nhiễm (**exposure bias**) giữa huấn luyện (teacher forcing) và kiểm thử.
- **Conformer:**
  - Kết hợp hài hòa giữa **Multi-Head Self-Attention** ("nhìn xa" nắm bắt ngữ cảnh toàn cục) và **Convolution Module** ("nhìn gần" trích xuất đặc trưng âm học cục bộ như formant transition, onset) trong cấu trúc khối kiểu Macaron.
- **Whisper:**
  - Kiến trúc Transformer Encoder-Decoder nhận đầu vào là Log-Mel Spectrogram.
  - Huấn luyện trên 680.000 giờ audio giám sát yếu (weakly supervised), hỗ trợ đa ngôn ngữ và đa nhiệm (ASR, speech translation, voice activity) thông qua hệ thống multitask prompt / special tokens với độ bền vững (robustness) zero-shot cao.
- **RNN-Transducer (RNN-T):** Kết hợp acoustic encoder với prediction network (mô hình hóa lịch sử nhãn) và joint network, tối ưu cho xử lý streaming thời gian thực.

## 3. Decoding, Tokenization & Đánh giá mô hình (WER/CER)

- **Chiến lược Decoding:**
  - **Greedy Decoding:** Chọn token có xác suất cao nhất ở từng bước; tốc độ tối ưu nhưng dễ rơi vào cực trị cục bộ.
  - **Beam Search:** Duy trì top-$k$ giả thuyết có điểm tích lũy cao nhất; tạo thế cân bằng giữa độ chính xác và chi phí tính toán.
- **External LM Fusion & Contextual Biasing:**
  - **Shallow Fusion:** Kết hợp xác suất âm học và mô hình ngôn ngữ ngoài: $\alpha \log P_{\text{ASR}} + \beta \log P_{\text{LM}}$.
  - **Contextual Biasing:** Tăng ưu tiên cho danh sách thực thể đặc thù (tên danh bạ, địa danh, câu lệnh hệ thống), cần cân đối với chứng cứ âm học để tránh hiện tượng ảo giác (hallucination).
- **Tokenization:**
  - Lựa chọn tối ưu là **Subword (BPE / SentencePiece)**, dung hòa giữa Character (chuỗi quá dài) và Word (nguy cơ Out-Of-Vocabulary cao).
  - Tiếng Việt phân tách âm tiết bằng khoảng trắng; bên cạnh **WER**, chỉ số **CER (Character Error Rate)** cung cấp đánh giá ổn định hơn.
- **Chỉ số WER & CER:**
  - Đánh giá tỷ lệ các phép chỉnh sửa:  
    $\text{WER} = \frac{S + D + I}{N}$  
    (với $S$: thay thế, $D$: xóa, $I$: chèn, $N$: tổng số từ chuẩn).
  - Bắt buộc thực hiện chuẩn hóa văn bản (**Text Normalization**) thống nhất về dấu câu, chữ hoa, số và từ viết tắt trước khi tính toán.

## 4. Liên hệ với đề tài Speech Understanding trên xe

- **Thách thức Cabin xe & Domain Shift:**
  - Tiếng ồn động cơ, gió, lốp xe, âm nhạc và hiện tượng dội âm tạo ra sự sai lệch phân phối (domain shift) lớn so với benchmark chuẩn.
  - Khẩu lệnh trên xe thường là **short utterances** ("dừng", "gọi mẹ"); việc module VAD cắt sai lệch 200–300 ms có thể làm mất hẳn phụ âm đầu hoặc cuối quan trọng.
- **Streaming ASR & Độ trễ (Latency):**
  - Xử lý audio theo chunk với bộ đệm look-ahead nhỏ.
  - Độ trễ trải nghiệm thực tế bao gồm toàn bộ chuỗi: VAD endpointing + buffering + RTF (Real-Time Factor) + decoding + sinh phản hồi downstream.
- **Tích hợp với Language Identification (LID):**
  - **Pipeline A:** Chạy model LID gọn nhẹ trên 1–3 giây đầu để điều hướng sang ASR chuyên ngữ (kiểm soát tốt tài nguyên và calibration).
  - **Pipeline B:** Sử dụng ASR đa ngôn ngữ tổng hợp để tự giải mã và chuyển dịch ngôn ngữ linh hoạt.
- **Phối hợp đảm bảo an toàn với Distress Detection:**
  - Transcript ASR là một nguồn có nguy cơ sai số; không được phụ thuộc duy nhất vào văn bản để đưa ra quyết định cứu nạn khẩn cấp.
  - Xây dựng kiến trúc dung hợp đa nhánh (**Multi-branch Fusion**): Nhánh âm thanh đi vào **Acoustic Event / Stress Branch** (bắt tiếng hét, bộc phát âm lượng, pitch tăng đột ngột) chạy song song với nhánh **ASR + Semantic Risk** (có kèm confidence score) trước khi vào tầng Safety Fusion.

## 5. Kết luận

- Bản chất cốt lõi của ASR là bài toán sequence transduction, giải quyết đồng thời việc ánh xạ biểu diễn, căn chỉnh vị trí (alignment) và giải mã chuỗi (decoding).
- Nắm vững cơ chế xử lý căn chỉnh của **CTC** thông qua ký hiệu blank và quy tắc collapse, lý tưởng cho các tác vụ cần tốc độ cao và suy luận nhanh.
- Hiểu cơ bản kiến trúc **Conformer** trong việc dung hòa pattern âm học cục bộ (CNN) với ngữ cảnh toàn cục (Attention), cùng thế mạnh đa nhiệm của **Whisper**.
- Nắm vững các kỹ thuật decoding, shallow fusion và phương pháp chuẩn hóa đánh giá chất lượng qua WER/CER.
- Thiết kế hệ thống trên xe đòi hỏi sự cân bằng nghiêm ngặt giữa độ trễ streaming, độ bền vững trước tiếng ồn cabin và cơ chế đa nhánh song song (Acoustic + Semantic) để đảm bảo độ tin cậy tuyệt đối cho Distress Detection.

## 6.Mini-project: Vietnamese–English Language Identification

## Mục tiêu
Xây dựng hệ thống **nhận dạng ngôn ngữ nói (LID)** để phân loại một đoạn âm thanh thành **English** hoặc **Vietnamese** mà không dùng transcript. Dataset sử dụng là **Google FLEURS** với hai cấu hình `en_us` và `vi_vn`, giữ nguyên official `train / validation / test` split.

## Dữ liệu và tiền xử lý
Audio được tải xuống disk/cache của Google Colab để tránh tràn RAM. Cột audio dùng `Audio(decode=False)` và được decode từng sample bằng `soundfile`. Train/validation chỉ lấy clip dài **≥ 3 s**, test chỉ lấy clip **≥ 5 s** để phục vụ thí nghiệm độ dài. Mỗi ngôn ngữ được lấy số lượng mẫu cân bằng.

Pipeline xử lý:

```text
FLEURS audio
→ mono waveform, 16 kHz
→ crop 3 s
→ STFT (25 ms window, 10 ms hop, FFT=512)
→ Magnitude
→ Power Spectrogram
→ 64-bin Mel Filterbank (80–7600 Hz)
→ Log-Mel Spectrogram
→ Normalization
→ CNN
→ English / Vietnamese
```

Train dùng **random crop 3 s**, validation/test dùng **center crop**. Dữ liệu được đọc theo generator và `tf.data`, không `np.stack()` toàn bộ corpus và không cache toàn dataset trong RAM.

## Mô hình
CNN cuối cùng gồm 4 khối convolution với số channel tăng dần `32 → 64 → 128 → 256`, kèm **Batch Normalization, ReLU, MaxPooling, Dropout và L2 regularization**. Sau đó dùng `GlobalAveragePooling2D`, `Dense(128)` và lớp output 2 logits. Optimizer là **Adam**, learning rate ban đầu `1e-4`, có `ReduceLROnPlateau` và `EarlyStopping`.

## Kết quả
Mô hình đạt tốt nhất ở khoảng **epoch 3**:

- Train accuracy: **~94.7%**
- Validation accuracy tốt nhất: **~66.2%**
- Test accuracy: **69.71%**
- Test Macro F1: **69.48%**

| Class | Precision | Recall | F1 |
|---|---:|---:|---:|
| English | 0.738 | 0.611 | 0.668 |
| Vietnamese | 0.668 | 0.784 | 0.721 |

Mô hình có dấu hiệu **overfitting mạnh**: train accuracy tăng lên gần 98% trong khi validation accuracy giảm xuống khoảng 26–32%. Vì vậy bottleneck hiện tại là khả năng tổng quát hóa hơn là thiếu năng lực mô hình.

### Thí nghiệm độ dài audio

| Duration | Accuracy | Macro F1 |
|---:|---:|---:|
| 0.5 s | 0.622 | 0.604 |
| 1.0 s | 0.665 | 0.657 |
| 2.0 s | 0.687 | 0.684 |
| 3.0 s | **0.697** | **0.695** |
| 5.0 s | 0.690 | 0.688 |

Hiệu năng tăng rõ từ **0.5 s → 3 s**, cho thấy nhiều speech context hơn giúp LID tốt hơn. Với model train trên đoạn 3 s, hiệu năng gần như bão hòa sau 3 s.

## Kết luận
Baseline **Log-Mel + CNN** đã học được đặc trưng phân biệt tiếng Việt và tiếng Anh, nhưng còn overfit đáng kể. Hướng phát triển tiếp theo nên tập trung vào **audio augmentation, SpecAugment, kiểm soát speaker/domain bias, VAD và so sánh CNN với Transformer / pretrained speech encoder** thay vì tiếp tục tăng kích thước CNN.
