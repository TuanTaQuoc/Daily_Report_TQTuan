# Báo cáo ngày 17/09/2026

## 1. Tổng quan công việc

Trong ngày 17/09/2026, công việc tập trung chủ yếu vào việc xây dựng và chuẩn hóa dữ liệu cho bài toán **Language Identification (LID)** trong project voice agent song ngữ Việt-Anh. Trọng tâm của ngày là bổ sung dữ liệu **code-switching Việt-Anh**, thống nhất lại cấu trúc thư mục dataset và thực hiện các bước kiểm tra, chuyển đổi định dạng audio trước khi đưa dữ liệu vào quá trình huấn luyện.

Bên cạnh phần chuẩn bị dữ liệu, một số nội dung liên quan đến xây dựng **baseline cho LID** và nghiên cứu **metric đánh giá** cũng đã được thực hiện. Qua quá trình kiểm tra dữ liệu, vấn đề mất cân bằng lớp giữa dữ liệu code-switching và các tập ngôn ngữ đơn đã được nhận diện và cần được xử lý ở các bước tiếp theo.

## 2. Các nhiệm vụ chính đã triển khai

### 2.1. Sinh dữ liệu code-switching Việt-Anh

- Xây dựng bộ script code-switching Việt-Anh để sinh dữ liệu audio.
- Hoàn thành **50 script**, gồm:
  - 30 câu ngắn;
  - 10 đoạn có độ dài trung bình;
  - 10 đoạn dài trên 3 câu.
- Sử dụng các công cụ tổng hợp giọng nói như **Vbee** hoặc **ElevenLabs** để sinh audio.
- Theo ghi chú trong ngày:
  - Mai thực hiện sinh 80 mẫu;
  - bản thân thực hiện sinh 30 mẫu.
- Các file sinh bằng ElevenLabs ở định dạng MP3 được chuyển đổi sang **WAV**.
- Sau khi sinh dữ liệu, các file được kiểm tra lại trước khi tải xuống.

Việc tạo thêm dữ liệu code-switching nhằm bổ sung các trường hợp người dùng chuyển đổi qua lại giữa tiếng Việt và tiếng Anh trong cùng một câu hoặc đoạn hội thoại. Đây là nhóm dữ liệu quan trọng đối với bài toán LID trong voice agent, vì dữ liệu thực tế không phải lúc nào cũng thuộc hoàn toàn một ngôn ngữ.

### 2.2. Chỉnh sửa và thống nhất cấu trúc dataset

Cấu trúc thư mục dataset đã được chỉnh sửa lại để tương đồng với cấu trúc dữ liệu đã đề xuất trước đó. Việc thống nhất cấu trúc giúp các tập dữ liệu từ nhiều nguồn có thể được xử lý theo cùng một pipeline, đồng thời giảm sự khác biệt giữa dữ liệu tự sinh và dữ liệu lấy từ các dataset bên ngoài.

Trong quá trình này, hai file transcript và hai tập audio cũng đã được gộp lại để tạo thành một tập dữ liệu thống nhất hơn cho bước tiền xử lý tiếp theo.

### 2.3. Kiểm tra và tiền xử lý dữ liệu audio

Các tập dữ liệu hiện có đã được kiểm tra về một số thuộc tính quan trọng của audio.

Kết quả kiểm tra:

- Ngoại trừ các file WAV code-switching tự sinh, các file audio còn lại đều có sample rate **16 kHz**.
- Các file code-switching tự sinh cần được resample về **16 kHz**.
- Tất cả audio đều là **mono-channel**.
- Các file `.flac` trong tập external test đã được chuyển sang định dạng **WAV, 16 kHz, mono**.
- Sau khi chuẩn hóa, toàn bộ audio trong data được xác nhận ở định dạng:
  - WAV;
  - 16 kHz;
  - mono.
- Kiểm tra thêm bits/sample cho thấy cần chuyển các file audio **32-bit sang 16-bit PCM** để thuận tiện hơn cho việc lưu trữ và huấn luyện.
- Nội dung audio cũng được kiểm tra sơ bộ nhằm phát hiện các trường hợp dữ liệu không phù hợp trước khi huấn luyện.

Như vậy, pipeline tiền xử lý hiện tại hướng tới việc đưa toàn bộ dữ liệu về một chuẩn chung là **WAV – 16 kHz – mono – 16-bit PCM**.

### 2.4. Nghiên cứu baseline và metric cho LID

Trong ngày đã hoàn thành phần nghiên cứu bổ sung về cách triển khai **baseline cho bài toán Language Identification**.

Song song với đó, các metric đánh giá cũng được tìm hiểu nhằm phục vụ việc đánh giá khả năng của thành phần LID và làm rõ mức độ cải thiện của hệ thống khi tích hợp thêm module nhận diện ngôn ngữ.

Ở giai đoạn hiện tại, mục tiêu chính vẫn là xây dựng được một baseline đủ rõ ràng để có cơ sở so sánh với các phiên bản mô hình hoặc pipeline được cải tiến sau này.

## 3. Nhận xét và cân nhắc

Một vấn đề được đặt ra trong ngày là có cần phân loại chi tiết nhiều dạng **code-switching** khác nhau hay không. Theo ghi chú hiện tại, việc chia nhỏ quá nhiều loại code-switching chưa mang lại nhiều ý nghĩa đối với mục tiêu chính của project. Do đó, hướng phù hợp hơn ở thời điểm này là tập trung vào khả năng nhận biết và xử lý các trường hợp audio có pha trộn ngôn ngữ, thay vì xây dựng taxonomy quá chi tiết ngay từ đầu.

Vấn đề đáng chú ý hơn là **class imbalance** trong dữ liệu. Số lượng mẫu code-switching hiện tại chỉ khoảng **100 mẫu**, trong khi mỗi nhóm ngôn ngữ đơn có khoảng **3.000 mẫu**. Sự chênh lệch này có thể khiến mô hình khó học được đặc trưng của nhóm mixed nếu sử dụng trực tiếp trong quá trình huấn luyện.

Theo kế hoạch hiện tại, cần tiếp tục tìm thêm dữ liệu mixed, trong đó có hướng nghiên cứu tập **ZeroSpeech**, với mục tiêu bổ sung khoảng **1.000 mẫu mixed hoặc nhiều hơn** nếu dữ liệu phù hợp.

Ngoài ra, việc upload toàn bộ thư mục data lên Google Drive để chia sẻ cho Mai vẫn chưa hoàn thành trong ngày.

## 4. Hướng công việc tiếp theo

- Upload thư mục dữ liệu đã chuẩn hóa lên Google Drive để chia sẻ trong nhóm.
- Tiếp tục kiểm tra chất lượng và nội dung của các file code-switching tự sinh.
- Hoàn thiện việc chuyển toàn bộ audio về chuẩn **WAV – 16 kHz – mono – 16-bit PCM**.
- Tìm thêm dữ liệu code-switching/mixed để giảm tình trạng class imbalance.
- Nghiên cứu khả năng sử dụng dữ liệu từ **ZeroSpeech**, ưu tiên bổ sung tối thiểu khoảng 1.000 mẫu mixed nếu phù hợp.
- Tiếp tục xây dựng baseline cho bài toán LID.
- Hoàn thiện bộ metric để đánh giá riêng thành phần Language Identification và phục vụ so sánh giữa các phương án.
- Nếu có điều kiện, trao đổi thêm với anh Kiên về hướng triển khai project cùng Mai.
- Nghiên cứu thêm dataset cho bài toán **Distress Identification** khi các nhiệm vụ chính đã ổn định.

## 5. Kết luận

Trong ngày 17/09/2026, phần lớn công việc tập trung vào việc bổ sung và chuẩn hóa dữ liệu cho bài toán LID. Bộ script code-switching Việt-Anh đã được xây dựng và dùng để sinh thêm audio. Cấu trúc dataset được điều chỉnh lại, các tập transcript và audio được gộp, đồng thời dữ liệu được kiểm tra và chuẩn hóa về sample rate, số channel, định dạng file và bits/sample.

Sau quá trình tiền xử lý, chuẩn audio mục tiêu được xác định là **WAV – 16 kHz – mono – 16-bit PCM**. Bên cạnh đó, phần nghiên cứu baseline và metric cho LID cũng đã được thực hiện. Vấn đề quan trọng nhất được phát hiện là sự mất cân bằng lớn giữa dữ liệu code-switching và dữ liệu ngôn ngữ đơn. Vì vậy, bước tiếp theo cần ưu tiên bổ sung thêm dữ liệu mixed trước khi tiến sâu hơn vào quá trình huấn luyện và đánh giá mô hình.
